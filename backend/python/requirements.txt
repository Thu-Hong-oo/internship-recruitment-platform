# Minimal inference requirements for Sentence-BERT on Alpine (keep build stable)
torch>=2.5.0
sentence-transformers>=2.6.0
numpy>=1.24.0

# ChromaDB and dependencies
chromadb>=0.4.0
# pysqlite3 will be built from source with newer SQLite in Dockerfile