/**
 * Visibility Enum
 * Domain: Shared
 * Represents visibility levels for content
 */
const Visibility = Object.freeze({
  PUBLIC: 'public',
  PRIVATE: 'private',
  RESTRICTED: 'restricted',
});

module.exports = Visibility;
