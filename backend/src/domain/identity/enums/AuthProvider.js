/**
 * AuthProvider Enum
 * Domain: Identity
 * Defines authentication providers for user authentication
 */
const AuthProvider = Object.freeze({
  EMAIL: 'email',
  GOOGLE: 'google',
  FACEBOOK: 'facebook',
  GITHUB: 'github',
  LINKEDIN: 'linkedin',
});

module.exports = AuthProvider;
