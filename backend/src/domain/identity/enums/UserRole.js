/**
 * UserRole Enum
 * Domain: Identity
 * Represents the role of a user in the system
 */
const UserRole = Object.freeze({
  CANDIDATE: 'candidate',
  EMPLOYER: 'employer',
  ADMIN: 'admin',
});

module.exports = UserRole;
