/**
 * OAuthProvider Enum
 * Domain: Identity
 * Defines OAuth providers for user authentication
 */
const OAuthProvider = Object.freeze({
  GOOGLE: 'google',
  FACEBOOK: 'facebook',
  GITHUB: 'github',
  LINKEDIN: 'linkedin'
});

module.exports = OAuthProvider;
