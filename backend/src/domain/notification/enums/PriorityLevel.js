/**
 * PriorityLevel Enum
 * Domain: Notification
 * Represents priority levels for notifications
 */
const PriorityLevel = Object.freeze({
  LOW: 'low',
  MEDIUM: 'medium',
  HIGH: 'high',
});

module.exports = PriorityLevel;
